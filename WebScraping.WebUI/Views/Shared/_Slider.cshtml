@using WebScraping.WebUI.ViewModel
@model ItemViewModel

@section styles
{
    <style>
    </style>
}


@*
    <div id="carouselExampleIndicators" class="carousel slide text-center mt-5 border" data-ride="carousel">
        <h5 class="h2 text-primary mb-5">Çok satan kitaplar</h5>
        <ol class="carousel-indicators">
            <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
        </ol>
        <div class="carousel-inner mb-5">

            @foreach (var book in Model.Books.Take(1).ToList())
            {
                <div class="carousel-item active">

                    <div class="col-md-2 col-sm-12">
                        <a href="#">
                            <div class="image">
                                <img src="@book.Image" class="img img-fluid" alt="Image" style="max-width: 100%;">
                            </div>
                        </a>
                    </div>

                    <!--.row-->
                </div>
            }

            @foreach (var book in Model.Books.Skip(1).Take(6).ToList())
            {
                <div class="carousel-item">
                    <div class="col-md-2 col-sm-12">
                        <a href="#">
                            <div class="image">
                                <img src="@book.Image" class="img img-fluid" alt="Image" style="max-width: 100%;">
                            </div>
                        </a>
                    </div>



                    <!--.row-->
                    <!--</div>-->
                    <!--.item-->
                    <!--</div>-->
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>

                </div>

            }
        </div>
    </div>
*@








<div class="top-content">
    <div class="container-fluid">
        <div id="carousel-example" class="carousel slide text-center" data-ride="carousel">
            <h5 class="h2 text-primary mb-5">Çok satan kitaplar</h5>
            <div class="carousel-inner row w-100 mx-auto" role="listbox">
                @for (int i = 0; i < Model.Books?.ToList().Count ; i++)
                {
                    <div class="carousel-item col-12 col-md-4 col-lg-2 @(i == 0 ? "active" : "")">
                        <img src="@(Model.Books?.ToList()[i].Image)" class="img-fluid mx-auto d-block img" alt="img">
                    </div>
                }


            </div>
            <a class="carousel-control-prev" href="#carousel-example" role="button" data-slide="prev" style="top: 40%;">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carousel-example" role="button" data-slide="next" style="top: 40%;">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>
</div>






